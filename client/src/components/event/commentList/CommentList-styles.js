import styled from "styled-components";
import * as styles from "../../../global-styles";

const StyledCommentList = styled.div`
  padding-top: 3rem;
  margin-top: 3rem;
  border-top: 1px solid ${styles.colorsLight.gray};
`;

export default StyledCommentList;
