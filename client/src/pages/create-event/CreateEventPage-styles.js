import styled from "styled-components";
import * as styles from "../../global-styles";

const StyledCreateEventPage = styled.div`
  width: 100%;
  height: 100%;
  background: ${styles.colors.white};
`;

export default StyledCreateEventPage;
